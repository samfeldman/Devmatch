
<div class="userslist">
	<h1 class="users">users</h1>
	<ul class="userlist">
		<% if @users %>
			<% @users.each do |user| %>
				<li><a href=<%= "/user/#{ user.id }" %>><%= "#{user.fname}" %> <%= "#{user.lname}" %></a></li>
			<% end %>
		<% end %>
	</ul>
</div>
